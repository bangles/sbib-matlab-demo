% applies the function "func" to every row of the matrix "matrix" and returns the result in c

function c = matrixfun(func, matrix)
    for i=1:size(matrix,1)
        c(i,:) = func( matrix(i,:) );
    end

